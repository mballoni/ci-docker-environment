#CI Environment

Small environment for development tests purposes

##Docker compose

This environment uses docker compose. For execution:

´´´sh
docker-compuse up
´´´

#Jenkinsfile

Simple jenkinsfile example with some jobs relying on gradle. Could easily be replaced with maven
